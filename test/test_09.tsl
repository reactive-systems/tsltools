// this specification should be split in two parts: a,b and c 
always assume {
    ([a <- val1()] && [b <- val2()]) -> X in1;
    [c <- val3()] -> X in2;
    F !in3;
}

always guarantee {
    F in1;
    F in2;
    
    in3 -> X !in1;
    in3 -> X !in2;
}

